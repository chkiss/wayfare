# Rate-limit zone for the wayfare vhost.
# Install as /etc/nginx/conf.d/wayfare-limit.conf — it must live in the http{}
# block, which is what conf.d/ is included into.
limit_req_zone $binary_remote_addr zone=wayfare:10m rate=30r/m;
